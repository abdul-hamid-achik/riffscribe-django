# Django and web framework
django>=5.0
django-htmx>=1.17
django-cors-headers>=4.3
whitenoise>=6.6
gunicorn>=21.2
python-decouple>=3.8

# Database
psycopg2-binary>=2.9

# Celery and task queue
celery>=5.3
redis>=5.0
flower>=2.0

# File handling
pillow>=10.0

# Audio processing core
numpy>=1.24,<2.0
scipy>=1.11
librosa>=0.10
soundfile>=0.12
pydub>=0.25
audioread>=3.0
resampy>=0.4

# Basic ML for audio (CPU-friendly versions)
torch>=2.0
torchaudio>=2.0

# Music processing
pretty-midi>=0.2
music21>=9.1
midiutil>=1.2

# Note: Advanced models need manual installation
# basic-pitch: pip install basic-pitch
# demucs: requires special installation
# crepe: pip install crepe
# madmom: requires system dependencies

# Guitar tab formats  
# guitarpro>=1.0  # Note: Not available on PyPI, install manually if needed

# Development tools
pytest>=7.4
pytest-django>=4.7
black>=23.12
ruff>=0.1
ipython>=8.18

# Optional advanced packages (install separately if needed)
# tensorflow>=2.15  # For basic-pitch
# mir-eval>=0.7  # For evaluation metrics